# 🤖 Система резервирования AI (Grok + Claude)

## 📊 Архитектура

**Основной API:** xAI Grok 2
**Резервный API:** Claude Sonnet 4.5

---

## ⚙️ Как работает автоматическое переключение

### 1️⃣ Попытка использовать Grok (основной)
```
Пользователь задает вопрос
    ↓
Бот пытается использовать xAI Grok API
    ↓
✅ Успех? → Ответ пользователю от Grok
```

### 2️⃣ Fallback на Claude (при сбое Grok)
```
❌ Grok недоступен (ошибка API)
    ↓
Бот автоматически переключается на Claude
    ↓
✅ Успех? → Ответ пользователю от Claude
```

### 3️⃣ Оба API недоступны
```
❌ Grok недоступен
    ↓
❌ Claude также недоступен
    ↓
Пользователю: "AI сервисы временно недоступны"
```

---

## 🎯 Используемые модели

| API | Модель | Назначение | Статус |
|-----|--------|-----------|--------|
| **xAI** | `grok-2-latest` | Основной AI для всех задач | 🟢 Основной |
| **Claude** | `claude-sonnet-4-5-20250929` | Резерв при сбое Grok | 🟡 Резервный |

---

## 📝 Логирование

Бот логирует какой API был использован:

```python
# При успешном ответе от Grok
✅ Ответ получен от xAI Grok

# При переключении на Claude
⚠️ xAI Grok недоступен: [причина]
🔄 Переключение на Claude Sonnet 4.5 (резерв)...
✅ Ответ получен от Claude Sonnet 4.5 (резерв)

# При полном сбое
❌ Claude также недоступен: [причина]
```

---

## 🔧 Где используется

Fallback работает для **ВСЕХ** запросов к AI:

1. **Классификация намерений** (определение типа вопроса)
2. **Анализ фотографий** (дефекты, конструкции)
3. **Анализ документов** (экспертные заключения)
4. **Текстовые вопросы** (консультации по нормативам)

---

## 💡 Преимущества системы

✅ **Высокая доступность** - если один API упал, работает другой
✅ **Автоматическое переключение** - пользователь не заметит сбоя
✅ **Логирование** - легко отследить проблемы
✅ **Прозрачность** - пользователь всегда получает ответ

---

## ⚠️ Требования

### Обязательные переменные окружения:

```env
# Основной API (обязательно)
XAI_API_KEY=xai-xxxxxxxxxxxx

# Резервный API (опционально, но рекомендуется)
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxx

# Telegram
TELEGRAM_BOT_TOKEN=xxxxxxxxxxxx
```

**Если нет ANTHROPIC_API_KEY:**
- Fallback на Claude не работает
- При сбое Grok бот выдаст ошибку

---

## 📈 Статистика использования

Проверить логи (`bot.log`), чтобы понять:
- Как часто используется Grok
- Сколько раз был fallback на Claude
- Есть ли проблемы с доступностью API

```bash
# Сколько раз использовался Grok
grep "Ответ получен от xAI Grok" bot.log | wc -l

# Сколько раз использовался Claude (резерв)
grep "Ответ получен от Claude" bot.log | wc -l

# Сколько раз оба API были недоступны
grep "Оба AI сервиса" bot.log | wc -l
```

---

## 🔄 Обновление (дата создания)

**Версия:** 3.0
**Дата:** 06.12.2025
**Автор:** Claude Code

---

**Powered by xAI Grok 2 + Claude Sonnet 4.5**
