# ⚙️ Автозапуск Telegram бота ТехНадзор

## ✅ АВТОЗАПУСК НАСТРОЕН!

Бот теперь будет **автоматически запускаться** каждый раз при входе в Windows!

---

## 📋 Что было сделано:

1. ✅ Создан скрипт `start_bot_silent.vbs` - запускает бота в фоновом режиме (без окна)
2. ✅ Скрипт добавлен в автозагрузку Windows
3. ✅ Бот будет запускаться незаметно при каждом входе в систему

---

## 🎯 Как это работает:

### При включении компьютера:
1. Вы входите в Windows
2. **Автоматически** запускается скрипт `TehNadzor_Bot.vbs`
3. Скрипт **в фоновом режиме** запускает бота
4. Бот начинает работать и отвечать в Telegram

### Никаких окон не появляется!
Бот работает **полностью в фоне**, вы даже не заметите, что он запустился.

---

## 🔍 Проверка автозапуска:

### Где находится скрипт автозапуска:
```
C:\Users\PC\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Startup\TehNadzor_Bot.vbs
```

### Как проверить, что бот в автозагрузке:
1. Нажмите `Win + R`
2. Введите: `shell:startup`
3. Нажмите Enter
4. Вы увидите файл `TehNadzor_Bot.vbs`

---

## 🛑 Как ОТКЛЮЧИТЬ автозапуск:

### Способ 1: Быстрый
Запустите файл:
```
C:\Users\PC\StroiNadzor\telegram_bot\remove_from_startup.bat
```

### Способ 2: Вручную
1. Нажмите `Win + R`
2. Введите: `shell:startup`
3. Удалите файл `TehNadzor_Bot.vbs`

---

## 🔄 Как ВКЛЮЧИТЬ обратно автозапуск:

Если вы отключили автозапуск, чтобы включить обратно:

### Вариант 1: Автоматически
Просто скопируйте файл обратно:
```bash
copy "C:\Users\PC\StroiNadzor\telegram_bot\start_bot_silent.vbs" "%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup\TehNadzor_Bot.vbs"
```

### Вариант 2: Вручную
1. Откройте папку: `C:\Users\PC\StroiNadzor\telegram_bot\`
2. Скопируйте файл `start_bot_silent.vbs`
3. Откройте папку автозагрузки (Win + R → `shell:startup`)
4. Вставьте скопированный файл туда

---

## ⚡ Управление ботом:

### Запустить бота ВРУЧНУЮ (если не хотите перезагружать ПК):
Двойной клик на:
```
C:\Users\PC\StroiNadzor\telegram_bot\start_bot_silent.vbs
```
Или:
```
C:\Users\PC\StroiNadzor\telegram_bot\start_bot.bat
```

### Остановить работающего бота:
1. Откройте Диспетчер задач (`Ctrl + Shift + Esc`)
2. Найдите процесс `python.exe` (с командой bot.py)
3. Нажмите "Снять задачу"

### Проверить, работает ли бот:
1. Откройте Telegram
2. Напишите боту @AINadzorBot
3. Отправьте `/start`
4. Если отвечает - работает! ✅

---

## 📊 Статус:

| Параметр | Значение |
|----------|----------|
| **Автозапуск** | ✅ Включен |
| **Режим работы** | Фоновый (без окна) |
| **Запуск при** | Входе в Windows |
| **Расположение** | Папка Startup |

---

## ⚠️ Важные моменты:

1. **Бот запустится только после входа в Windows**
   - Не сразу при включении компьютера
   - А после того, как вы введете пароль

2. **Интернет должен быть подключен**
   - Бот работает через интернет
   - Без интернета не будет отвечать

3. **Антивирус может спросить разрешение**
   - Первый раз антивирус может попросить подтверждение
   - Разрешите запуск Python и скрипта

4. **Задержка 5-10 секунд**
   - Бот запускается не мгновенно
   - Подождите несколько секунд после входа в систему

---

## 🔧 Техническая информация:

### Что делает скрипт `start_bot_silent.vbs`:
```vbs
1. Переходит в папку с ботом
2. Запускает команду: python bot.py
3. Режим: скрытое окно (0)
4. Не ждет завершения (False)
```

### Файлы автозапуска:
- `start_bot_silent.vbs` - основной скрипт (в папке бота)
- `TehNadzor_Bot.vbs` - копия в автозагрузке (запускается при входе)

---

## ✅ Готово!

Теперь ваш Telegram бот **всегда будет онлайн**, пока компьютер включен!

**После перезагрузки компьютера проверьте, что бот автоматически запустился:**
1. Перезагрузите ПК
2. Войдите в систему
3. Подождите 10-15 секунд
4. Напишите боту в Telegram
5. Если отвечает - все работает! 🎉

---

**Дата настройки:** 14.11.2025
**Версия:** 1.0
**Статус:** ✅ Активен
