# Руководство по генерации изображений в StroiNadzorAI

## Обзор

Бот StroiNadzorAI теперь поддерживает генерацию изображений и схем через **Gemini AI** (fallback при отсутствии OpenAI API).

## Как это работает

### Автоматический Fallback

Система использует следующую стратегию:

1. **DALL-E 3** (OpenAI) - если доступен `OPENAI_API_KEY`
   - Генерирует фотореалистичные изображения
   - Высокое качество, но требует API ключ
   - Стоимость: ~$0.04 за изображение

2. **Gemini 2.5 Flash** - fallback (всегда доступен)
   - Генерирует технические схемы
   - Использует уже настроенный `GEMINI_API_KEY`
   - Бесплатно (в пределах квоты)

### Текущая конфигурация

```
GEMINI_API_KEY=AIzaSyBth8MGnZvXNB1288eXWsUCj7bbbt0IRRM ✅ (настроен)
OPENAI_API_KEY=не настроен ⚠️
```

**Результат:** Бот использует Gemini AI для генерации схем.

## Использование в Telegram

### Команда /generate

```
/generate три колодца соединены между собой в разрезе от здания
```

**Что происходит:**
1. Бот пытается использовать DALL-E 3
2. При отсутствии ключа переключается на Gemini
3. Генерирует техническую схему размером 1024x1024
4. Отправляет пользователю с описанием

### Автоматическая генерация

Бот автоматически определяет запросы на генерацию изображений:

**Триггеры:**
- "нарисуй..."
- "покажи..."
- "можешь прислать картинку..."
- "как выглядит..."
- "визуализируй..."

**Пример:**
```
Пользователь: Можешь прислать картинку с примером где от здания
три колодца соединены между собой в разрезе

Бот: [Генерирует схему через Gemini AI]
```

## Что генерируется

### Типы изображений (Gemini)

1. **Технические схемы**
   - Простые линейные схемы
   - С текстовым описанием элементов
   - Размер: 1024x1024px
   - Формат: PNG

2. **Строительные диаграммы**
   - Блочные диаграммы элементов
   - С соединительными линиями
   - Размер: 1200x800px
   - Цветовая схема: белый фон, синие блоки

3. **Схемы с аннотациями**
   - Технические пояснения
   - Ключевые пункты
   - Структурированный текст

### Примеры сгенерированных элементов

```
┌─────────────────────────────────────────┐
│     СТРОИТЕЛЬНАЯ СХЕМА                  │
│                                         │
│  • Колодец №1 (от здания)              │
│  • Соединительная труба                 │
│  • Колодец №2 (промежуточный)          │
│  • Соединительная труба                 │
│  • Колодец №3 (крайний)                │
│                                         │
│  Создано с помощью Gemini AI           │
└─────────────────────────────────────────┘
```

## Технические детали

### Архитектура

**Модуль:** `image_generator.py`
```python
def generate_construction_image(user_request: str, use_hd: bool = False):
    # 1. Пытается DALL-E 3
    try:
        result = generate_image_dalle3(prompt)
        if result:
            return result
    except:
        pass  # OPENAI_API_KEY не найден

    # 2. Fallback на Gemini
    if dalle_result is None:
        generator = get_gemini_generator()
        image_data = await generator.generate_schematic_image(user_request)
        return result
```

**Модуль:** `gemini_image_gen.py`
```python
class GeminiImageGenerator:
    def __init__(self, api_key):
        self.vision_model = genai.GenerativeModel('models/gemini-2.5-flash')

    async def generate_schematic_image(self, description):
        # 1. Используем Gemini для создания описания
        prompt = f"Создай описание схемы: {description}"
        response = self.vision_model.generate_content(prompt)

        # 2. Рисуем схему с помощью Pillow
        img = Image.new('RGB', (1024, 1024), 'white')
        draw = ImageDraw.Draw(img)

        # 3. Добавляем текст из Gemini
        # ...

        return BytesIO(img)
```

### Используемые библиотеки

- **google-generativeai** - для Gemini API
- **Pillow (PIL)** - для рисования схем
- **asyncio** - для асинхронной работы
- **openai** (опционально) - для DALL-E 3

### Модели Gemini

**Доступные модели:**
```
- models/gemini-2.5-pro (лучшее качество)
- models/gemini-2.5-flash ✅ (используется, быстрая)
- models/gemini-2.0-flash
```

**Текущая:** `gemini-2.5-flash`
- Оптимальная скорость
- Хорошее качество
- Поддержка мультимодальности

## Тестирование

### Запуск тестов

```bash
cd C:/Users/PC/StroiNadzorAI
python test_image_generation.py
```

**Ожидаемый вывод:**
```
=== TEST IMAGE GENERATION (GEMINI FALLBACK) ===

OPENAI_API_KEY: NOT SET
GEMINI_API_KEY: SET
Gemini generator: LOADED

Request: три колодца соединены между собой в разрезе от здания

Generating image...

[SUCCESS] Image generated!
  Model: gemini-schematic
  Size: 1024x1024
  Quality: schematic
  Timestamp: 2025-12-02 21:59:56
  Image data size: 10920 bytes
  Saved to: test_generated_image.png

=== TEST PASSED ===
```

### Проверка результата

Откройте файл `test_generated_image.png` - вы увидите техническую схему с описанием.

## Добавление DALL-E 3 (опционально)

Если хотите использовать DALL-E 3 для фотореалистичной генерации:

### 1. Получите API ключ OpenAI

Зарегистрируйтесь на https://platform.openai.com/api-keys

### 2. Добавьте в .env

```bash
OPENAI_API_KEY=sk-your-openai-api-key-here
```

### 3. Перезапустите бота

Теперь бот будет использовать DALL-E 3 для фотореалистичных изображений.

## Сравнение: DALL-E vs Gemini

| Характеристика | DALL-E 3 | Gemini Schematic |
|---------------|----------|------------------|
| Качество | Фотореалистично | Схематично |
| Стоимость | $0.04/изображение | Бесплатно (квота) |
| Скорость | ~10-30 сек | ~5-10 сек |
| API ключ | Требуется | Уже настроен ✅ |
| Типы | Фото, 3D, рендеры | Схемы, диаграммы |

## Примеры использования

### 1. Запрос схемы колодцев

**Вход:**
```
Можешь прислать картинку с примером где от здания
три колодца соединены между собой в разрезе
```

**Выход:**
```
🎨 Изображение создано

📝 Ваш запрос: три колодца соединены между собой...

🤖 Описание схемы:
Техническая схема: три колодца соединены...

⚙️ Параметры:
• Модель: gemini-schematic
• Размер: 1024x1024
• Качество: schematic

⏰ 2025-12-02 21:59:56

💡 Схема создана с помощью Gemini AI
_Для фотореалистичной генерации добавьте OPENAI_API_KEY_
```

### 2. Запрос узла соединения

**Вход:**
```
/generate узел соединения балки и колонны
```

**Выход:**
- Техническая схема с элементами узла
- Подписи и размеры
- Структурированное описание

### 3. Запрос дефекта

**Вход:**
```
нарисуй трещину в бетонном фундаменте
```

**Выход:**
- Схема с трещиной
- Технические характеристики
- Рекомендации

## Логи и отладка

### Включение детального логирования

В `bot.py`:
```python
logging.basicConfig(level=logging.INFO)
```

### Ключевые логи

```
INFO:image_generator:🎨 Запрос на генерацию: ...
INFO:image_generator:📝 Улучшенный промпт: ...
ERROR:image_generator:Ошибка генерации DALL-E 3: OPENAI_API_KEY не найден
INFO:image_generator:🔄 Переключаюсь на Gemini AI...
INFO:image_generator:📌 Gemini генератор: True
INFO:gemini_image_gen:✅ Схематическое изображение создано
INFO:image_generator:✅ Схема Gemini создана успешно
```

## Известные ограничения

1. **Gemini схемы** - простые, не фотореалистичные
2. **Размер изображения** - фиксированный (1024x1024)
3. **Цветовая схема** - базовая (черно-белая с акцентами)
4. **Детализация** - ограничена возможностями Pillow

## Roadmap

Планируемые улучшения:

- [ ] Добавить Imagen 3 для фотореалистичной генерации через Gemini
- [ ] Поддержка загрузки изображений для аннотирования
- [ ] Цветные схемы с настраиваемой палитрой
- [ ] Экспорт в векторные форматы (SVG)
- [ ] Пакетная генерация нескольких вариантов
- [ ] Интеграция с CAD инструментами

## Поддержка

При возникновении проблем:

1. Проверьте наличие `GEMINI_API_KEY` в `.env`
2. Убедитесь, что установлены зависимости: `pip install -r requirements.txt`
3. Запустите тест: `python test_image_generation.py`
4. Проверьте логи в `bot.log`
5. Откройте issue на GitHub

## Статус

✅ **Работает:** Gemini AI fallback для генерации схем
⚠️ **Опционально:** DALL-E 3 для фотореалистичных изображений
🔄 **В разработке:** Imagen 3 интеграция

---

**Версия:** 1.0
**Дата:** 2025-12-02
**Автор:** Claude Code
