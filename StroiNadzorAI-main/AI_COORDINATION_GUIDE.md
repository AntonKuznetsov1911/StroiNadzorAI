# 🤖 Руководство по AI Координации

## 📋 Обзор

Новая система координации AI для StroiNadzorAI использует **три AI сервиса**:

1. **XAI (Grok)** - Главный AI, координатор
2. **OpenAI (DALL-E 3)** - Генерация изображений
3. **Anthropic (Claude Sonnet 4.5)** - Резервный AI при сбое XAI

## 🎯 Архитектура

```
Пользователь → XAI (анализирует запрос)
                  ↓
       ┌──────────┴──────────┐
       ↓                     ↓
   Текстовый ответ    Нужно изображение?
       ↓                     ↓
   Отправка          DALL-E генерирует
                     параллельно с ответом
```

### Как это работает:

1. **Пользователь отправляет запрос** (текст или голос)
2. **XAI анализирует запрос** и определяет:
   - Нужна ли генерация изображения
   - Какой промпт передать DALL-E
3. **Одновременно:**
   - XAI отправляет текстовый ответ с техническим описанием
   - DALL-E генерирует изображение (если нужно)
4. **Fallback:** Если XAI не работает → используется Claude

## 🔧 Настройка

### 1. API Ключи

Добавьте в `.env` файл:

```env
# Обязательно
XAI_API_KEY=xai-ваш_ключ_здесь
TELEGRAM_BOT_TOKEN=ваш_telegram_токен

# Для генерации изображений (опционально, но рекомендуется)
OPENAI_API_KEY=sk-proj-ваш_openai_ключ

# Для резервного AI (опционально)
ANTHROPIC_API_KEY=sk-ant-api03-ваш_anthropic_ключ
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

Убедитесь, что установлены:
- `httpx>=0.27.0` (для XAI)
- `openai>=1.0.0` (для DALL-E)
- `anthropic>=0.74.0` (для Claude)

### 3. Тестирование

Запустите тестовый скрипт:

```bash
python test_ai_system.py
```

Скрипт проверит:
- ✅ Наличие API ключей
- ✅ Работу XAI (Grok)
- ✅ Работу DALL-E
- ✅ Работу Claude
- ✅ Координацию AI
- ✅ Параллельную генерацию

## 📦 Новые файлы

### 1. `ai_coordinator.py`

Главный модуль координации:
- `analyze_request_and_coordinate()` - анализ запроса и координация AI
- `generate_text_and_image_parallel()` - параллельная генерация
- `get_ai_status()` - проверка статуса AI сервисов

### 2. `openai_dalle.py`

Модуль генерации изображений:
- `generate_image_dalle()` - генерация через DALL-E 3
- `generate_construction_plan()` - специализированная генерация схем
- `generate_sewer_system_diagram()` - генерация схем канализации

### 3. `bot_ai_integration.py`

Интеграция с bot.py:
- `handle_text_with_ai_coordination()` - новый обработчик текста
- `ai_status_command()` - команда /ai_status
- `test_dalle_command()` - команда /test_dalle

### 4. `test_ai_system.py`

Тестовый скрипт для проверки всех функций.

## 🔌 Интеграция в bot.py

### Вариант 1: Полная интеграция (рекомендуется)

Замените в `bot.py` функцию обработки текста:

```python
# В начале файла после импортов:
from bot_ai_integration import (
    handle_text_with_ai_coordination,
    ai_status_command,
    test_dalle_command
)

# В функции handle_text замените блок генерации ответа на:
await handle_text_with_ai_coordination(
    update=update,
    context=context,
    conversation_history=conversation_history,
    system_prompt=system_prompt,
    user_id=user_id,
    question=question,
    add_message_to_history_async=add_message_to_history_async
)

# В функции main() добавьте команды:
application.add_handler(CommandHandler("ai_status", ai_status_command))
application.add_handler(CommandHandler("test_dalle", test_dalle_command))
```

### Вариант 2: Постепенная интеграция

Используйте модули напрямую в нужных местах кода.

## 🎨 Примеры использования

### Пример 1: Простой вопрос

**Пользователь:** "Какой класс бетона нужен для фундамента?"

**Система:**
1. XAI анализирует → определяет: изображение НЕ нужно
2. XAI отвечает текстом с нормативами
3. Пользователь получает ответ

### Пример 2: Запрос с изображением

**Пользователь:** "Нарисуй схему колодцев от административного здания до центральной канализации по прямой в три колодца"

**Система:**
1. XAI анализирует → определяет: нужно изображение
2. **Параллельно:**
   - XAI описывает текстом: расстояния, глубины, нормы и т.д.
   - XAI создает промпт для DALL-E на английском
3. Пользователь получает текстовый ответ
4. DALL-E генерирует схему (10-30 сек)
5. Пользователь получает изображение с описанием

### Пример 3: Голосовое сообщение

**Пользователь:** (голосом) "Нарисуй схему фундамента"

**Система:**
1. OpenAI Whisper распознает голос → текст
2. XAI анализирует текст → нужно изображение
3. Параллельная генерация текста и изображения
4. Пользователь получает оба результата

## 📊 Новые команды бота

### `/ai_status`

Показывает статус всех AI сервисов:
```
🤖 Статус AI Сервисов

✅ XAI (Grok): Доступен (главный AI)
✅ OpenAI (DALL-E): Доступен (генерация изображений)
✅ Anthropic (Claude): Доступен (резервный AI)

✅ Основной AI работает нормально
```

### `/test_dalle`

Тестирует генерацию изображения через DALL-E:
- Генерирует тестовую схему фундамента
- Показывает время генерации
- Сохраняет результат

## 🛡️ Fallback система

### Сценарий 1: XAI недоступен

```
Запрос → XAI (ошибка) → Anthropic Claude → Ответ
```

Пользователь получает сообщение:
```
💬 Ответ (🧠 ANTHROPIC):
[Ответ от Claude]
```

### Сценарий 2: DALL-E недоступен

```
Запрос → XAI → Текст ✅
              → DALL-E (ошибка) → Сообщение об ошибке
```

Пользователь получает текстовое описание с пометкой, что изображение не сгенерировано.

### Сценарий 3: Все AI недоступны

```
Запрос → XAI (ошибка) → Claude (ошибка) → Сообщение об ошибке
```

Пользователь видит:
```
❌ Извините, AI временно недоступны
Пожалуйста, попробуйте позже.
```

## 💡 Особенности работы

### Определение необходимости изображения

Система автоматически определяет по ключевым словам:
- "нарисуй", "покажи", "сгенерируй"
- "создай изображение/картинку/фото"
- "визуализируй", "изобрази"
- "схему", "план", "чертеж"

### Промпт-инженеринг

XAI создает детальный промпт для DALL-E на английском:
```
📝 Промпт для генерации изображения:
"Technical construction diagram showing sewer manhole
layout from administrative building to central sewage
system. Three manholes in a straight line. Professional
blueprint style with measurements, top view perspective,
engineering drawing with annotations."
```

### Параллельная обработка

Текст и изображение генерируются **одновременно**:
- Пользователь получает текст сразу (1-3 сек)
- Изображение приходит следом (10-30 сек)
- Не нужно ждать генерации изображения для получения текста

## 📈 Производительность

### Скорость ответа

- **Только текст:** 1-3 секунды (XAI)
- **Текст + изображение:**
  - Текст: 1-3 секунды
  - Изображение: +10-30 секунд

### Стоимость

- **XAI (Grok):** $5.00 / 1M input, $15.00 / 1M output
- **DALL-E 3:**
  - Standard 1024x1024: $0.040 / изображение
  - HD 1024x1024: $0.080 / изображение
- **Claude:** $3.00 / 1M input, $15.00 / 1M output (только fallback)

## 🔍 Логирование

Система логирует все важные события:

```
🚀 Запуск AI координации...
📊 Статус AI: {'xai_available': True, 'openai_available': True, ...}
✅ Получен ответ от XAI
🎨 Запрос требует генерации изображения
📝 Извлечен промпт для DALL-E: Technical construction...
🎨 Запуск DALL-E для генерации изображения...
✅ Изображение успешно сгенерировано
✅ Изображение успешно отправлено
```

## ❓ FAQ

### Нужны ли все три API ключа?

**Минимум:** XAI_API_KEY (обязательно)

**Рекомендуется:**
- XAI_API_KEY + OPENAI_API_KEY (для генерации изображений)
- XAI_API_KEY + ANTHROPIC_API_KEY (для fallback)

**Оптимально:** Все три

### Что если нет OPENAI_API_KEY?

Генерация изображений будет недоступна. Пользователь получит только текстовое описание.

### Что если нет ANTHROPIC_API_KEY?

При сбое XAI бот не сможет ответить. Рекомендуется иметь fallback.

### Голосовые сообщения работают?

Да! Голосовые сообщения распознаются через OpenAI Whisper (если есть OPENAI_API_KEY) или Vosk (офлайн). После распознавания обрабатываются как текст.

### Как проверить, что все работает?

Запустите:
```bash
python test_ai_system.py
```

Или в боте:
```
/ai_status
/test_dalle
```

## 🆘 Решение проблем

### Ошибка: "XAI клиент недоступен"

**Решение:** Проверьте XAI_API_KEY в `.env`

### Ошибка: "DALL-E недоступен"

**Решение:**
1. Проверьте OPENAI_API_KEY в `.env`
2. Убедитесь, что баланс OpenAI положительный
3. Проверьте лимиты API

### Ошибка: "Все AI недоступны"

**Решение:**
1. Проверьте все API ключи
2. Проверьте балансы
3. Проверьте интернет соединение
4. Проверьте логи: `tail -f bot.log`

### Изображения не генерируются

**Проверьте:**
1. `is_dalle_available()` возвращает True
2. OPENAI_API_KEY корректный
3. Баланс OpenAI положительный
4. Запрос содержит триггерные слова ("нарисуй", "покажи" и т.д.)

## 📞 Поддержка

При проблемах проверьте:
1. ✅ API ключи в `.env`
2. ✅ Зависимости установлены
3. ✅ Балансы на платформах
4. ✅ Логи бота (`bot.log`)
5. ✅ Результаты теста (`test_ai_system.py`)

---

**Автор:** Claude Code
**Дата:** 2025-12-18
**Версия:** 1.0 (AI Coordination System)
