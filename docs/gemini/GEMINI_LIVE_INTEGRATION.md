# 🔌 Интеграция Gemini Live API в StroiNadzorAI

## Быстрая инструкция по подключению голосового ассистента

---

## 📋 ЧТО НУЖНО СДЕЛАТЬ

### 1. Установить зависимости

```bash
pip install websockets>=12.0
```

Или обновить все зависимости:
```bash
pip install -r requirements.txt
```

---

### 2. Добавить в bot.py

Откройте `bot.py` и добавьте импорты в начало файла:

```python
# Добавьте эти импорты после существующих
from gemini_live_bot_integration import (
    init_voice_assistant,
    register_voice_assistant_handlers
)
```

---

### 3. Инициализация в main()

Найдите функцию `main()` и добавьте инициализацию голосового ассистента:

```python
async def main():
    """Запуск бота"""
    # ... существующий код ...

    # Инициализация AI провайдеров
    init_claude()
    init_gemini()

    # ДОБАВЬТЕ ЭТУ СТРОКУ:
    init_voice_assistant()  # Инициализация Gemini Live API

    # ... остальной код ...
```

---

### 4. Регистрация обработчиков

В той же функции `main()` найдите место где регистрируются обработчики команд и добавьте:

```python
async def main():
    # ... существующий код ...

    # Регистрация существующих обработчиков
    application.add_handler(CommandHandler("start", start_command))
    application.add_handler(CommandHandler("help", help_command))
    # ... другие обработчики ...

    # ДОБАВЬТЕ ЭТУ СТРОКУ:
    register_voice_assistant_handlers(application)  # Регистрация Live API обработчиков

    # ... остальной код ...
```

---

### 5. Проверка работоспособности

Запустите бота:
```bash
python bot.py
```

В логах должны появиться строки:
```
✅ Голосовой ассистент Gemini Live API инициализирован
✅ Обработчики голосового ассистента зарегистрированы
```

---

### 6. Тестирование

В Telegram боте выполните:
```
/voice_chat
```

Должен появиться ответ:
```
🎤 ГОЛОСОВОЙ АССИСТЕНТ ЗАПУЩЕН

Теперь вы можете:
• 🎤 Отправлять голосовые сообщения
• 📸 Отправлять фото с вопросами
• 💬 Писать текстом
```

Отправьте голосовое сообщение - должен прийти голосовой ответ!

---

## 🔍 ПОЛНЫЙ ПРИМЕР ИНТЕГРАЦИИ

### bot.py (фрагмент)

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
StroiNadzorAI - Telegram бот консультант по строительным нормам
"""

import logging
from telegram import Update
from telegram.ext import (
    Application,
    CommandHandler,
    MessageHandler,
    filters,
    ContextTypes
)

# Существующие импорты
from claude_handler import init_claude
from gemini_handler import init_gemini
from optimized_handlers import handle_question

# НОВЫЕ ИМПОРТЫ для Gemini Live API
from gemini_live_bot_integration import (
    init_voice_assistant,
    register_voice_assistant_handlers
)

# ... остальной код ...

async def main():
    """Запуск бота"""
    logger.info("🚀 Запуск StroiNadzorAI v2.0")

    # Инициализация AI
    init_claude()
    init_gemini()
    init_voice_assistant()  # ← ДОБАВЛЕНО

    # Создание приложения
    application = Application.builder().token(TELEGRAM_TOKEN).build()

    # Регистрация обработчиков
    application.add_handler(CommandHandler("start", start_command))
    application.add_handler(CommandHandler("help", help_command))
    # ... другие обработчики ...

    register_voice_assistant_handlers(application)  # ← ДОБАВЛЕНО

    # Запуск бота
    logger.info("✅ Бот запущен и готов к работе")
    await application.run_polling(allowed_updates=Update.ALL_TYPES)

if __name__ == "__main__":
    import asyncio
    asyncio.run(main())
```

---

## 📝 КОМАНДЫ ДЛЯ ПОЛЬЗОВАТЕЛЕЙ

После интеграции пользователям станут доступны:

| Команда | Описание |
|---------|----------|
| `/voice_chat` | Начать голосовой разговор |
| `/voice_help` | Справка по голосовому ассистенту |
| `/stop` | Завершить разговор |

---

## ⚙️ НАСТРОЙКА (опционально)

### Изменить голос бота

По умолчанию используется голос "Aoede" (женский, профессиональный).

Чтобы изменить, отредактируйте `gemini_live_api.py`:

```python
def __init__(
    self,
    api_key: Optional[str] = None,
    model: str = "gemini-2.0-flash-exp",
    voice: str = "Charon",  # ← Измените здесь
    # ... остальные параметры ...
):
```

Доступные голоса:
- `Aoede` - женский, профессиональный (по умолчанию)
- `Charon` - мужской, глубокий
- `Fenrir` - мужской, энергичный
- `Kore` - женский, спокойный
- `Puck` - нейтральный, быстрый

---

### Изменить системную инструкцию

Системная инструкция определяет поведение голосового ассистента.

По умолчанию используется инструкция для строительного эксперта.

Чтобы изменить, отредактируйте метод `_get_default_system_instruction()` в `gemini_live_api.py`.

---

## 🐛 РЕШЕНИЕ ПРОБЛЕМ

### Ошибка: "GOOGLE_API_KEY не найден"

**Решение:**
```bash
export GOOGLE_API_KEY="your_api_key_here"
```

Для Railway добавьте в переменные окружения.

---

### Ошибка: "websockets module not found"

**Решение:**
```bash
pip install websockets>=12.0
```

---

### Ошибка: "WebSocket connection failed"

**Причины:**
1. Проверьте интернет соединение
2. Убедитесь что GOOGLE_API_KEY корректный
3. Проверьте firewall (должен разрешать WebSocket)

**Решение:**
```bash
# Тест Live API
python gemini_live_api.py

# Должно показать:
# ✅ Gemini Live API работает!
```

---

### Голосовые ответы не приходят

**Проверьте:**
1. Логи бота: `tail -f bot.log`
2. Запущена ли Live сессия: `/voice_chat`
3. Формат аудио (должен быть OGG, MP3, WAV или PCM)

**Решение:**
- Остановите сессию: `/stop`
- Запустите заново: `/voice_chat`

---

### Задержка > 1 секунды

**Причины:**
- Медленное интернет соединение
- Перегрузка Gemini API
- Большой размер аудио файла

**Решение:**
- Проверьте скорость интернета
- Используйте более короткие сообщения
- Улучшите качество связи

---

## 📊 МОНИТОРИНГ

### Логи

Все взаимодействия с Gemini Live API логируются:

```python
logger.info("✅ Live сессия запущена (ID: {session_id})")
logger.info("🎤 Отправлено аудио: {bytes} байт")
logger.info("🔊 Получено аудио: {bytes} байт")
```

### Статистика сессии

После завершения разговора бот показывает статистику:

```
📊 Статистика сессии:
• Отправлено сообщений: 5
• Получено сообщений: 5
• Аудио фрагментов отправлено: 12
• Аудио фрагментов получено: 15
• Ошибок: 0
```

---

## 💰 СТОИМОСТЬ

**Gemini 2.0 Flash Live API:**
- Аудио вход: $0.075 / 1000 единиц
- Аудио выход: $0.30 / 1000 единиц
- 1 единица ≈ 1 минута разговора

**Пример:**
- 1000 консультаций по 2 минуты = $0.75
- Очень доступно для профессионального использования!

---

## 📚 ДОПОЛНИТЕЛЬНЫЕ РЕСУРСЫ

- **Руководство пользователя:** `GEMINI_LIVE_GUIDE.md`
- **Код Live API:** `gemini_live_api.py`
- **Интеграция с ботом:** `gemini_live_bot_integration.py`
- **Документация Gemini:** https://ai.google.dev/gemini-api/docs/live-api

---

## ✅ CHECKLIST ИНТЕГРАЦИИ

- [ ] Установлен websockets
- [ ] Добавлены импорты в bot.py
- [ ] Добавлен init_voice_assistant() в main()
- [ ] Добавлен register_voice_assistant_handlers()
- [ ] Проверены логи при запуске
- [ ] Протестирована команда /voice_chat
- [ ] Отправлено тестовое голосовое сообщение
- [ ] Получен голосовой ответ
- [ ] Проверена статистика сессии

---

**Создано:** Claude Sonnet 4.5
**Дата:** 2025-12-20
**Проект:** StroiNadzorAI v2.0

**Готово к развертыванию! 🚀**
