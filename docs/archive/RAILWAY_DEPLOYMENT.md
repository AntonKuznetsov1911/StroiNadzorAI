# 🚀 Развертывание на Railway

## ✅ Текущий статус

**СтройНадзорAI теперь работает 24/7 на Railway!**

- **Project**: calm-dream
- **Service**: worker
- **Environment**: production
- **URL**: https://railway.com/project/44e38344-d25e-4d9a-9b1d-a20cd5111473

---

## 📋 Что было сделано

### 1. Конфигурация проекта

Созданы необходимые файлы:

**Procfile**:
```
worker: python bot.py
```

**railway.json**:
```json
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
```

**runtime.txt**:
```
python-3.11.9
```

### 2. Переменные окружения

Настроены в Railway:
- ✅ `TELEGRAM_BOT_TOKEN` - токен Telegram бота
- ✅ `ANTHROPIC_API_KEY` - ключ API Claude

### 3. Развертывание

```bash
# Подключение к проекту (уже выполнено)
railway link

# Деплой на Railway
railway up

# Проверка логов
railway logs --tail 50
```

---

## 🎯 Преимущества Railway

✅ **24/7 работа** - бот работает постоянно, компьютер не нужен
✅ **Автоматический перезапуск** - при сбоях Railway перезапускает бот (до 10 попыток)
✅ **Логи в реальном времени** - `railway logs` для мониторинга
✅ **Переменные окружения** - безопасное хранение токенов и ключей
✅ **GitHub интеграция** - автодеплой при пуше (опционально)

---

## 📊 Мониторинг

### Проверка статуса:
```bash
railway status
```

### Просмотр логов:
```bash
# Последние 50 строк
railway logs --tail 50

# В реальном времени
railway logs --follow
```

### Управление переменными окружения:
```bash
# Просмотр
railway variables

# Добавить новую
railway variables set KEY=value
```

---

## 🔄 Обновление бота

При внесении изменений в код:

```bash
# 1. Коммит изменений в Git
git add .
git commit -m "Описание изменений"
git push origin main

# 2. Деплой на Railway
railway up
```

Railway автоматически:
1. Скачает новый код
2. Установит зависимости из requirements.txt
3. Запустит новую версию бота
4. Остановит старую версию

---

## ⚙️ Конфигурация

### requirements.txt (автоматически устанавливается)
```
python-telegram-bot==21.0.1
anthropic>=0.40.0
python-dotenv==1.0.1
reportlab==4.0.7
python-docx==1.1.0
```

### Файлы конфигурации Railway:
- `Procfile` - команда запуска
- `railway.json` - настройки билда и деплоя
- `runtime.txt` - версия Python
- `.railwayignore` - игнорируемые файлы при деплое

---

## 🐛 Решение проблем

### Конфликт при getUpdates
**Проблема**: `Conflict: terminated by other getUpdates request`

**Решение**:
1. Убедитесь, что локально бот не запущен: `tasklist | findstr python`
2. Остановите все локальные процессы: `taskkill /F /IM python.exe`
3. Подождите 30-60 секунд для синхронизации

### Ошибки при деплое
**Проблема**: Deployment failed

**Решение**:
1. Проверьте логи: `railway logs`
2. Убедитесь, что все зависимости в requirements.txt
3. Проверьте переменные окружения: `railway variables`

### Бот не отвечает
**Проблема**: Бот не реагирует на команды

**Решение**:
1. Проверьте логи: `railway logs --follow`
2. Перезапустите сервис через Railway Dashboard
3. Проверьте токен Telegram: `railway variables`

---

## 📞 Поддержка Railway

- **Dashboard**: https://railway.app
- **Документация**: https://docs.railway.app
- **Discord**: https://discord.gg/railway
- **CLI команды**: `railway --help`

---

## ✅ Итоги

**До развертывания на Railway:**
- ❌ Нужно держать компьютер включенным 24/7
- ❌ Бот останавливается при перезагрузке
- ❌ Сложно мониторить

**После развертывания на Railway:**
- ✅ Работает 24/7 без компьютера
- ✅ Автоматический перезапуск при сбоях
- ✅ Логи и мониторинг в реальном времени
- ✅ Простое обновление через `railway up`

---

**Дата развертывания:** 25.11.2025
**Версия бота:** v2.6
**Railway Project ID:** 44e38344-d25e-4d9a-9b1d-a20cd5111473
**Powered by:** Claude Sonnet 4.5 + Railway
