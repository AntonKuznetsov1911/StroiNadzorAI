# Установка и настройка Stable Diffusion Web UI для StroiNadzorAI

Это руководство поможет вам установить и настроить **Stable Diffusion Web UI** для генерации качественных строительных схем и изображений в вашем Telegram боте.

---

## Оглавление

1. [Системные требования](#системные-требования)
2. [Установка Stable Diffusion Web UI](#установка-stable-diffusion-web-ui)
3. [Скачивание моделей](#скачивание-моделей)
4. [Запуск с API](#запуск-с-api)
5. [Настройка бота](#настройка-бота)
6. [Облачные варианты](#облачные-варианты)
7. [Устранение проблем](#устранение-проблем)

---

## Системные требования

### Минимальные требования
- **ОС**: Windows 10/11, Linux, macOS
- **RAM**: 8 GB (рекомендуется 16 GB)
- **GPU**: NVIDIA с 6+ GB VRAM (рекомендуется)
- **Место на диске**: 20+ GB

### Рекомендуемые требования
- **GPU**: NVIDIA RTX 3060 или лучше (8+ GB VRAM)
- **RAM**: 16+ GB
- **Место на диске**: 50+ GB (для нескольких моделей)

> **Примечание**: Можно запускать на CPU, но генерация будет медленной (5-10 минут на изображение).

---

## Установка Stable Diffusion Web UI

### Вариант 1: Автоматическая установка (Windows)

1. **Скачайте Stable Diffusion Web UI**:
   - Перейдите на: https://github.com/AUTOMATIC1111/stable-diffusion-webui
   - Скачайте релиз или клонируйте репозиторий:
   ```bash
   git clone https://github.com/AUTOMATIC1111/stable-diffusion-webui.git
   cd stable-diffusion-webui
   ```

2. **Запустите установщик**:
   ```bash
   webui-user.bat
   ```
   - При первом запуске скачаются все зависимости (может занять 10-30 минут)

### Вариант 2: Установка на Linux

1. **Установите зависимости**:
   ```bash
   sudo apt update
   sudo apt install wget git python3 python3-venv python3-pip
   ```

2. **Клонируйте репозиторий**:
   ```bash
   git clone https://github.com/AUTOMATIC1111/stable-diffusion-webui.git
   cd stable-diffusion-webui
   ```

3. **Запустите установщик**:
   ```bash
   ./webui.sh
   ```

### Вариант 3: Docker (Все платформы)

```bash
docker pull ghcr.io/automatic1111/stable-diffusion-webui:latest

docker run -d \
  -p 7860:7860 \
  -v sd-models:/app/models \
  -v sd-outputs:/app/outputs \
  --gpus all \
  ghcr.io/automatic1111/stable-diffusion-webui:latest
```

---

## Скачивание моделей

### Рекомендуемые модели для технических схем

1. **Stable Diffusion 3 Medium** (Лучшее качество)
   - Скачать: https://huggingface.co/stabilityai/stable-diffusion-3-medium
   - Размер: ~5 GB
   - Отлично для технических чертежей

2. **Stable Diffusion XL 1.0** (Универсальная)
   - Скачать: https://huggingface.co/stabilityai/stable-diffusion-xl-base-1.0
   - Размер: ~7 GB
   - Хорошее качество, быстрая генерация

3. **RealisticVision** (Для фотореализма)
   - Скачать: https://civitai.com/models/4201/realistic-vision
   - Размер: ~2 GB
   - Если нужны реалистичные изображения дефектов

### Как установить модель

1. Скачайте файл модели (`.safetensors` или `.ckpt`)
2. Поместите в папку: `stable-diffusion-webui/models/Stable-diffusion/`
3. Перезапустите Web UI
4. Модель появится в списке моделей в интерфейсе

---

## Запуск с API

### Включение API в Web UI

1. **Отредактируйте файл запуска**:

   **Windows** (`webui-user.bat`):
   ```batch
   set COMMANDLINE_ARGS=--api --listen --port 7860
   call webui.bat
   ```

   **Linux** (`webui-user.sh`):
   ```bash
   export COMMANDLINE_ARGS="--api --listen --port 7860"
   ./webui.sh
   ```

2. **Параметры запуска**:
   - `--api` - включает API
   - `--listen` - разрешает подключения извне (не только localhost)
   - `--port 7860` - порт API (можно изменить)
   - `--xformers` - ускорение генерации (если есть GPU)
   - `--medvram` - для GPU с 6-8 GB VRAM
   - `--lowvram` - для GPU с 4-6 GB VRAM

3. **Запустите Web UI**:
   ```bash
   # Windows
   webui-user.bat

   # Linux
   ./webui-user.sh
   ```

4. **Проверьте доступность API**:
   - Откройте: http://127.0.0.1:7860
   - API документация: http://127.0.0.1:7860/docs

---

## Настройка бота

### 1. Обновите `.env` файл

Добавьте в ваш `.env` файл:

```env
# Stable Diffusion API URL
SD_API_URL=http://127.0.0.1:7860

# Если SD на другом сервере:
# SD_API_URL=http://your-server-ip:7860
```

### 2. Запустите бота

```bash
python bot.py
```

### 3. Проверьте работу

Отправьте боту:
```
нарисуй трещину в стене
```

Бот должен:
- Использовать Claude для улучшения промпта
- Сгенерировать изображение через SD
- Отправить вам результат

---

## Облачные варианты

Если у вас нет мощного ПК, можно использовать облачные сервисы:

### 1. **Google Colab** (Бесплатно с ограничениями)

Notebook для запуска SD Web UI:
```
https://github.com/camenduru/stable-diffusion-webui-colab
```

**Плюсы**: Бесплатно, мощный GPU
**Минусы**: Ограничение по времени (12 часов), нужно перезапускать

### 2. **Vast.ai** (От $0.10/час)

1. Зарегистрируйтесь: https://vast.ai
2. Выберите GPU (рекомендуется RTX 3060+)
3. Разверните Docker контейнер с SD Web UI
4. Используйте публичный IP в `SD_API_URL`

**Плюсы**: Дешево, без ограничений
**Минусы**: Нужна оплата

### 3. **RunPod** (От $0.20/час)

1. Зарегистрируйтесь: https://runpod.io
2. Выберите шаблон "Stable Diffusion Web UI"
3. Запустите инстанс
4. Используйте URL инстанса в `SD_API_URL`

**Плюсы**: Простая настройка, предустановленные модели
**Минусы**: Дороже чем Vast.ai

### 4. **Replicate API** (Pay-as-you-go)

Альтернатива локальному SD - используйте API:

```python
# Установите: pip install replicate

import replicate

output = replicate.run(
    "stability-ai/stable-diffusion:db21e45d3f7023abc2a46ee38a23973f6dce16bb082a930b0c49861f96d1e5bf",
    input={"prompt": "technical drawing of wall crack"}
)
```

Добавьте в `.env`:
```env
REPLICATE_API_TOKEN=your_token_here
```

---

## Устранение проблем

### Проблема: "Connection refused" при подключении к SD API

**Решение**:
1. Убедитесь, что SD Web UI запущен
2. Проверьте, что используется флаг `--api`
3. Проверьте порт: `netstat -an | findstr 7860`

### Проблема: "CUDA out of memory"

**Решение**:
1. Уменьшите размер изображения в `stable_diffusion_api.py`:
   ```python
   width=768, height=768  # Вместо 1024x1024
   ```
2. Используйте флаг `--medvram` или `--lowvram`
3. Уменьшите количество шагов:
   ```python
   steps=20  # Вместо 30
   ```

### Проблема: Генерация слишком медленная

**Решение**:
1. Используйте более быстрый sampler: `Euler a` или `DPM++ 2M`
2. Уменьшите количество шагов: `steps=15-20`
3. Включите xformers: добавьте `--xformers` в параметры запуска
4. Используйте меньшую модель (SD 1.5 вместо XL)

### Проблема: Плохое качество изображений

**Решение**:
1. Улучшите промпт через Claude (уже встроено)
2. Увеличьте количество шагов: `steps=30-40`
3. Поэкспериментируйте с `cfg_scale` (7-9 для технических изображений)
4. Попробуйте другие модели (SD XL, SD 3)

---

## Тестирование интеграции

### 1. Проверка SD API

```bash
curl -X POST http://127.0.0.1:7860/sdapi/v1/txt2img \
  -H "Content-Type: application/json" \
  -d '{
    "prompt": "technical drawing of construction defect",
    "steps": 20,
    "width": 512,
    "height": 512
  }'
```

### 2. Проверка через бота

Отправьте боту любую из команд:
- `/generate нарисуй схему армирования фундамента`
- `покажи как выглядит трещина в стене`
- `создай чертёж узла примыкания`

### 3. Логи бота

Проверьте логи бота на наличие:
```
✅ Stable Diffusion генератор загружен
🚀 Используем Stable Diffusion для генерации
✅ SD генерация успешна
```

---

## Дополнительные ресурсы

- **Официальный репозиторий**: https://github.com/AUTOMATIC1111/stable-diffusion-webui
- **Wiki**: https://github.com/AUTOMATIC1111/stable-diffusion-webui/wiki
- **Модели**: https://civitai.com / https://huggingface.co
- **Сообщество**: https://www.reddit.com/r/StableDiffusion/

---

## Поддержка

Если у вас возникли проблемы:

1. Проверьте логи бота: `bot.log`
2. Проверьте логи SD Web UI в консоли
3. Убедитесь, что все переменные окружения установлены
4. Проверьте, что SD API доступен: http://127.0.0.1:7860

**Важно**: Первая генерация может занять больше времени (загрузка модели в память).
