# 🐘 Настройка PostgreSQL на Railway

## Шаг 1: Создание PostgreSQL базы данных

1. **Откройте Railway проект:**
   ```
   https://railway.app/project/[ваш-проект-id]
   ```

2. **Добавьте PostgreSQL:**
   - Нажмите **"New"** → **"Database"** → **"Add PostgreSQL"**
   - Railway автоматически создаст базу данных
   - Подождите 1-2 минуты, пока БД развернется

3. **Скопируйте DATABASE_URL:**
   - Нажмите на PostgreSQL сервис
   - Перейдите на вкладку **"Variables"**
   - Найдите `DATABASE_URL`
   - Нажмите кнопку копировать

4. **Добавьте DATABASE_URL в ваш проект:**
   - Перейдите к вашему боту (сервис StroiNadzorAI)
   - Откройте **"Variables"**
   - Нажмите **"New Variable"**
   - Имя: `DATABASE_URL`
   - Значение: вставьте скопированный URL
   - Сохраните

## Шаг 2: Автоматический деплой

После пуша кода на GitHub:

```bash
git add .
git commit -m "Добавлена поддержка PostgreSQL"
git push origin main
```

Railway автоматически:
1. Обнаружит новый код
2. Установит `asyncpg==0.29.0`
3. Создаст таблицы в PostgreSQL
4. Запустит бота с новой БД

## Шаг 3: Миграция существующих данных (опционально)

Если у вас уже есть JSON файлы с историей:

1. **Локально:**
   ```bash
   # Добавьте DATABASE_URL в .env
   echo "DATABASE_URL=postgresql://..." >> .env

   # Запустите миграцию
   python migrate_to_postgres.py
   ```

2. **Или на Railway:**
   - Railway автоматически мигрирует при первом запуске (если есть JSON файлы)

## Проверка работы

1. **Проверьте логи Railway:**
   ```
   Railway Dashboard → Ваш проект → StroiNadzorAI → Deployments → View logs
   ```

2. **Ищите строки:**
   ```
   ✅ PostgreSQL модуль загружен
   ✅ PostgreSQL база данных инициализирована
   ```

3. **Если видите:**
   ```
   ⚠️ PostgreSQL не доступен, используется JSON
   ```
   Это значит DATABASE_URL не настроен.

## Преимущества PostgreSQL

✅ **Постоянное хранение** - данные НЕ удаляются при перезапуске
✅ **Автоматические бэкапы** - Railway делает бэкапы каждые 24 часа
✅ **Быстрый поиск** - индексы для мгновенного поиска
✅ **Масштабирование** - от 10 до 10,000+ пользователей
✅ **Транзакции** - нет потери сообщений

## Бесплатный план Railway

PostgreSQL на Railway **бесплатно** включает:
- 500 MB хранилища (~100,000 сообщений)
- 5 GB bandwidth
- Автоматические бэкапы (7 дней)
- Неограниченные запросы

## Troubleshooting

### Проблема: "DATABASE_URL не найден"

**Решение:**
1. Проверьте, что PostgreSQL создан
2. Скопируйте DATABASE_URL из PostgreSQL сервиса
3. Добавьте в Variables вашего бота

### Проблема: "Connection refused"

**Решение:**
1. Подождите 2-3 минуты после создания PostgreSQL
2. Перезапустите бота (Deployments → Restart)

### Проблема: "Relation does not exist"

**Решение:**
Таблицы не созданы. Перезапустите бота:
```
Railway → Deployments → Restart
```

## Rollback на JSON

Если нужно вернуться к JSON:

1. **Удалите DATABASE_URL:**
   ```
   Railway → Variables → DATABASE_URL → Delete
   ```

2. **Перезапустите бота**

Бот автоматически вернется к JSON хранилищу.

---

**Готово!** 🎉
Ваш бот теперь использует надежное PostgreSQL хранилище.
